import substripeTexture from '../../src/textures/substripeTexture'

describe('substripe texture', () => {
	it('does nothing at the moment', () => {
		expect(substripeTexture()).toBe(undefined)
	})
})
